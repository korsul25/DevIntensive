<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorP_primary</item>
        <item name="colorPrimaryDark">@color/colorrimaryDark</item>
        <item name="colorAccent">@color/colorccent</item>
    </style>

</resources>
